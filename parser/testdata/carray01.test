==== 400

  DROP TABLE IF EXISTS t1;
  CREATE TABLE t1(a INT PRIMARY KEY, b INT) WITHOUT ROWID;
  WITH c(x) AS (
    VALUES(1)
    UNION
    SELECT x+1 FROM (carray NATURAL FULL JOIN carray(t1.b)), t1, c
  )
  SELECT * FROM c;
----
stmt 0 ok
stmt 27 ok
stmt 86 err 1 -1 217 no such table: carray
