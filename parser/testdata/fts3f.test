==== 1.0

  CREATE VIRTUAL TABLE ft USING fts3(x);
  BEGIN;
    INSERT INTO ft VALUES('a one'), ('b one'), ('c one');
----
stmt 0 ok
stmt 41 ok
stmt 50 err 1 -1 108 no such table: ft
==== 1.2

  BEGIN;
    INSERT INTO ft VALUES('a one'), ('b one'), ('c one');
----
stmt 0 ok
stmt 9 err 1 -1 67 no such table: ft
==== 1.3

  SELECT docid, optimize(ft) FROM ft WHERE ft MATCH 'one'
----
stmt 0 err 1 -1 59 no such table: ft
