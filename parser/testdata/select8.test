==== select8-1.1

    SELECT DISTINCT artist,sum(timesplayed) AS total      
    FROM songs      
    GROUP BY LOWER(artist)      
    LIMIT 1 OFFSET 1
----
stmt 0 err 1 -1 no such table: songs
==== select8-1.2

    SELECT DISTINCT artist,sum(timesplayed) AS total      
    FROM songs      
    GROUP BY LOWER(artist)      
    LIMIT 2 OFFSET 1
----
stmt 0 err 1 -1 no such table: songs
==== select8-1.3

    SELECT DISTINCT artist,sum(timesplayed) AS total      
    FROM songs      
    GROUP BY LOWER(artist)      
    LIMIT -1 OFFSET 2
----
stmt 0 err 1 -1 no such table: songs
